import { createStyles } from '@material-ui/core/styles';

export const styles = theme =>
	createStyles({
		root: {
			borderStyle: 'solid',
			borderWidth: 5,
			borderColor: '#dddddd',
			padding: '10px',
		},
		date: {
			color: '#888',
			fontSize: 14
		},
		bodyText: {
			
		}
	});
