import { createStyles } from '@material-ui/core/styles';

export const styles = theme =>
	createStyles({
		root: {
			marginTop: 20,
			marginBottom: 10,
		},
		image: {
			width: '100%',
			height: 300,
			marginBottom: 10,
		},
		eventDetails: {
			color: '#3f3f5a',
		},
	});
