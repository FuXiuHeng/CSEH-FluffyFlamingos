import { createStyles } from '@material-ui/core/styles';

export const styles = theme =>
	createStyles({
		root: {
			marginTop: 20,
		},
		image: {
			width: '100%',
			height: 180,
			marginBottom: 5,
		},
		eventDetails: {
			color: '#3f3f5a',
		},
	});
