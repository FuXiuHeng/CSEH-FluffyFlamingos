import * as _colors from './colors';
import * as _dimensions from './dimensions';
import * as _fonts from './fonts';
import * as _sizes from './sizes';

export const colors = _colors;
export const dimensions = _dimensions;
export const fonts = _fonts;
export const sizes = _sizes;
